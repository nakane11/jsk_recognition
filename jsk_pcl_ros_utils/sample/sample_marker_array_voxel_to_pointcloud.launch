<launch>
  <arg name="gui" default="true"/>
  <arg name="BAG_FILEPATH" default="$(find jsk_pcl_ros_utils)/sample/data/bunny_marker_array_d155b9ce5188fbaf89745847fd5882d7.bag" />

  <param name="/use_sim_time" value="true"/>

  <arg name="MANAGER" default="nodelet_manager" />
  <arg name="launch_manager" default="true" />

  <node name="$(arg MANAGER)"
        pkg="nodelet" type="nodelet"
        args="manager"
        if="$(arg launch_manager)"
        output="screen" />

  <node name="rosbag_play"
        pkg="rosbag" type="play"
        args="$(arg BAG_FILEPATH) --clock --loop"/>

  <node name="marker_array_voxel_to_pointcloud"
        pkg="nodelet" type="nodelet"
        args="load jsk_pcl_utils/MarkerArrayVoxelToPointCloud $(arg MANAGER)"
        respawn="true">
    <remap from="~input" to="/octree_voxel_grid/output_marker_array"/>
  </node>

  <group if="$(arg gui)">
    <node name="rviz"
          pkg="rviz" type="rviz"
          args="-d $(find jsk_pcl_ros_utils)/sample/config/sample_marker_arrray_voxel_to_pointcloud.rviz"/>
  </group>
</launch>
